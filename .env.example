# Скопируйте этот файл в .env и вставьте куки из браузера.
# DevTools → Network → запрос к api.petrovich.ru → Headers → Cookie.
APP_PARSER_COOKIES={"cookie_name":"cookie_value"}

# Опционально: дополнительные заголовки в JSON-формате.
# APP_PARSER_HEADERS={"Header-Name":"Header value"}
